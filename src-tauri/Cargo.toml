[package]
name = "iShell"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "ishell_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1.0", features = ["v4", "fast-rng", "macro-diagnostics"] }
tauri-plugin-shell = "2.3.3"
tauri-plugin-fs = "2.0.0"
tauri-plugin-dialog = "2"
tauri-plugin-os = "2"
# 保持 sqlx 兼容性，建议在 Android 环境下确保不使用动态链接的系统库
sqlx = { version = "0.8", features = [ "sqlite", "runtime-tokio" ] }

# 🟢 [修改] 核心 SSH 库：使用纯 Rust 的 russh 替代 ssh2
# 这样可以彻底删除 OpenSSL 相关的复杂配置
russh = "0.40"
russh-keys = "0.40"

# 🟢 [修改] 网络请求：强制使用 rustls-tls
# 这能确保 reqwest 不会偷偷去调用安卓系统里不存在的 native-tls (OpenSSL)
reqwest = { version = "0.11", default-features = false, features = ["json", "stream", "multipart", "rustls-tls"] }

tokio = { version = "1", features = ["full"] }
sysinfo = "0.30"
aes-gcm = "0.10"
pbkdf2 = "0.12"
hmac = "0.12"
sha2 = "0.10"
rand = "0.8"
base64 = "0.21"
chrono = { version = "0.4", features = ["serde"] }
zip = "0.6"
walkdir = "2"
regex = "1"
urlencoding = "2"

# 🔴 [已移除] 不兼容安卓或引起编译错误的插件/库
# tauri-plugin-autostart = "2.5.1"
# tauri-plugin-clipboard = "2.1.11"
# font-kit = "0.11"
